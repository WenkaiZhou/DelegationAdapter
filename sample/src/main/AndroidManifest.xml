<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kevin.delegationadapter.sample">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".home.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".multitype.news.common.NewsActivity" />
        <activity android:name=".multitype.news.binding.BindingNewsActivity" />
        <activity android:name=".multitype.chat.binding.BindingChatActivity" />
        <activity android:name=".multitype.chat.common.ChatActivity" />
        <activity android:name=".fallback.FallbackActivity" />
        <activity android:name=".multidataandtype.binding.BindingGoodsActivity" />
        <activity android:name=".header.HeaderActivity" />
        <activity android:name=".footer.FooterActivity" />
        <activity android:name=".samedata.SameDataActivity" />
        <activity android:name=".pro.meishijie.MeishijieActivity" />
        <activity android:name=".refreshload.RefreshLoadActivity" />
    </application>

</manifest>